{
  "name": "open_task",
  "description": "Open the task with the given id in the client UI so the user can see it. Only call this when the current run declared it.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "description": "Id of the task to open"
      }
    }
  }
}
